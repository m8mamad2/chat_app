import 'dart:math' as math;
import 'package:flutter/material.dart';

// Color kRandomColor = Color((math.Random().nextDouble() * 0xFFFFFF).toInt()).withOpacity(1.0);
// Color kRandomColor = Colors.primaries[math.Random().nextInt(Colors.primaries.length)];

